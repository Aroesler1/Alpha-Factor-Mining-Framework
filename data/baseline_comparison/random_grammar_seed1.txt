# random-grammar-seed1: 54 expressions sampled from
# ExpressionSanitizer's grammar, matched to the Sonnet set's structure.
# Regenerate with scripts/sp500_run_baseline_comparison.py --seeds 5
TS_ARGMIN(MIN(TS_MAX(($return + $close), 63), $open), 252)
(TS_DECAY_LINEAR(($open * $dollar_volume), 3) * IF(($dollar_volume > $close), $low, ($high + $return)))
EMA(TS_RANK(($dollar_volume * $dollar_volume), 21), 63)
IF((POWER($volume, 0.25) < 0), ($return + $volume), MIN($volume, $open))
(MAX($low, ($open * $return)) * (EMA(($volume * $high), 10) - BOUND($close, -3, 3)))
(BOUND(($dollar_volume - $volume), -3, 3) / (CS_ZSCORE(IF((($high * $close) <= ($dollar_volume * $return)), $high, $open)) + 1e-8))
COUNT(((LN(($open + $low)) / (TS_COV($high, ($return * $open), 5) + 1e-8)) < $return), 252)
IF((($volume + $dollar_volume) < 0), IF_ELSE(($open >= ($low - $open)), ($low * $return), $low), ($low - $dollar_volume))
COUNT(((TS_COVARIANCE(($return * $low), $low, 10) * (BOUND($volume, -3, 3) + TS_COVARIANCE($open, ($close + $open), 5))) > $close), 3)
TS_ARGMIN((BOUND($open, -3, 3) / ((POWER($volume, 1.5) / (((TS_COVARIANCE($close, $close, 63) * BOUND($low, -3, 3)) * ABS(($low * $dollar_volume))) + 1e-8)) + 1e-8)), 10)
(MAX(($volume * $return), MIN(($volume + $high), ($low + $open))) + TS_STD(($dollar_volume * $volume), 10))
LOG(BOUND(TS_MEAN($return, 63), -3, 3))
CS_ZSCORE(($low - $volume))
(BOUND($return, -3, 3) + COUNT(($dollar_volume < 0), 5))
MAX(BOUND(($dollar_volume * $close), -3, 3), TS_COVARIANCE(TS_COV($low, ($close * $high), 3), ($open * $high), 10))
MAX(MAX($low, $dollar_volume), BOUND(TS_STD($low, 5), -3, 3))
(COUNT((TS_SUM(($volume * $volume), 21) < 0), 21) - (MIN($low, ($return * $high)) * SQRT($return)))
EMA($volume, 252)
MAX(($low - $close), MAX(IF_ELSE(($dollar_volume >= 0), ($volume - $open), $volume), ($dollar_volume - $volume)))
(BOUND($low, -3, 3) - MAX(($open - $return), IF(($return > 0), SQRT(($dollar_volume * $return)), $high)))
IF_ELSE(($dollar_volume > 0), $dollar_volume, BOUND(TS_COVARIANCE($close, $return, 5), -3, 3))
(IF_ELSE((($return + $low) < 0), ($volume * $open), $open) / (COUNT(($open < $dollar_volume), 3) + 1e-8))
POWER(TS_COV($return, $dollar_volume, 5), 2)
(TS_COVARIANCE($close, $high, 5) * MIN(($close - $high), $dollar_volume))
((TS_CORR(($volume - $open), $close, 252) + MIN($low, ($dollar_volume - $high))) * IF(($close < ($low * $close)), $high, $low))
POWER((BOUND(TS_COVARIANCE(($open + $open), ($dollar_volume - $volume), 63), -3, 3) + (BOUND(($open - $volume), -3, 3) * TS_DELTA(($open * $close), 5))), 3)
BOUND(POWER($return, 0.25), -3, 3)
LOG(POWER($open, 3))
TS_ARGMIN(($open - $high), 5)
((POWER($volume, 1.5) - MAX(($return * $close), $dollar_volume)) * BOUND(($close + $volume), -3, 3))
MIN($close, TS_MIN($high, 3))
(TS_COV($volume, ($open * $volume), 10) / (POWER(TS_COVARIANCE(MIN(($high + $volume), $high), COUNT((TS_SUM($low, 21) < 0), 63), 21), 0.25) + 1e-8))
(TS_SUM(($low + $close), 10) + MIN(($return + $open), ($close - $high)))
(BOUND(($volume * $low), -3, 3) - (MAX($close, $close) - IF((($high - $high) > 0), ($return + $open), $high)))
SQRT(IF_ELSE((($dollar_volume * $open) <= $low), $high, $return))
POWER(BOUND(TS_MIN($open, 5), -3, 3), 3)
(IF_ELSE(($dollar_volume < $return), $open, $dollar_volume) * COUNT((($dollar_volume + $close) > 0), 10))
(TS_COV(($volume * $dollar_volume), $open, 63) + TS_COV(($dollar_volume + $low), MAX($volume, $low), 5))
MAX(TS_COVARIANCE($high, ($close + $close), 3), TS_CORR($high, ($volume * $return), 3))
(POWER($dollar_volume, 2) - ABS((COUNT(((TS_CORR(($return * $high), $low, 3) + TS_RANK($low, 10)) >= ($open + $high)), 10) * POWER($low, 0.5))))
TS_COVARIANCE(($dollar_volume * $volume), MIN($close, ($high + $dollar_volume)), 63)
(TS_CORR(($close * $low), ($volume * $low), 10) * IF((($dollar_volume + $volume) > ($high - $high)), $high, POWER($return, 2)))
TS_CORR($close, $close, 3)
(SQRT($return) / (TS_COV($open, $return, 21) + 1e-8))
ZSCORE(IF_ELSE((($return + $return) <= ($volume - $volume)), ($dollar_volume * $open), ($dollar_volume + $dollar_volume)))
TS_MIN(IF(($open < 0), COUNT(($close <= 0), 252), $dollar_volume), 5)
IF_ELSE((TS_COVARIANCE($low, ($close * $return), 252) < ($open - $volume)), $high, $low)
(MAX($high, $open) * BOUND(IF(($volume >= $volume), $volume, ($close + $dollar_volume)), -3, 3))
BOUND(TS_RANK(($high + $low), 10), -3, 3)
MAX(($high - $low), ($return * $volume))
(POWER($close, 3) - POWER($open, 1))
IF_ELSE((($volume * $high) > $high), ($dollar_volume + $dollar_volume), COUNT((($dollar_volume + $high) <= ($return - $low)), 252))
POWER(TS_COVARIANCE($volume, $return, 21), 2)
IF_ELSE((TS_CORR($close, $close, 5) <= $low), POWER(($dollar_volume + $open), 1.5), ($high * $volume))
