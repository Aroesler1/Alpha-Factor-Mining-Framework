# Generated by: random-grammar-seed1
# Scored on: daily_bars.parquet (3,279,854 rows after membership filter)
# Membership: /Users/alex/Desktop/Quant_Projects/QuantaAlpha/LLMStrat/data/us_equities/reference/sp500_membership_daily.parquet
# Gates: |t| >= 2.0, |corr| <= 0.7, max 10 factors
# Candidates considered: 54
signals:
  mined_expressions:
    - "(BOUND(($volume * $low), -3, 3) - (MAX($close, $close) - IF((($high - $high) > 0), ($return + $open), $high)))"
    - "POWER((BOUND(TS_COVARIANCE(($open + $open), ($dollar_volume - $volume), 63), -3, 3) + (BOUND(($open - $volume), -3, 3) * TS_DELTA(($open * $close), 5))), 3)"
    - "-((TS_COV($volume, ($open * $volume), 10) / (POWER(TS_COVARIANCE(MIN(($high + $volume), $high), COUNT((TS_SUM($low, 21) < 0), 63), 21), 0.25) + 1e-8)))"
    - "-(POWER(TS_COVARIANCE($volume, $return, 21), 2))"
    - "-(POWER(TS_COV($return, $dollar_volume, 5), 2))"
    - "TS_ARGMIN(MIN(TS_MAX(($return + $close), 63), $open), 252)"
    - "-((TS_CORR(($close * $low), ($volume * $low), 10) * IF((($dollar_volume + $volume) > ($high - $high)), $high, POWER($return, 2))))"
    - "-((POWER($close, 3) - POWER($open, 1)))"
    - "-(BOUND(TS_RANK(($high + $low), 10), -3, 3))"
    - "POWER(BOUND(TS_MIN($open, 5), -3, 3), 3)"
