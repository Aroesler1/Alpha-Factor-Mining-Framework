# Generated by: random-grammar-seed2
# Scored on: daily_bars.parquet (3,279,854 rows after membership filter)
# Membership: /Users/alex/Desktop/Quant_Projects/QuantaAlpha/LLMStrat/data/us_equities/reference/sp500_membership_daily.parquet
# Gates: |t| >= 2.0, |corr| <= 0.7, max 10 factors
# Candidates considered: 54
signals:
  mined_expressions:
    - "-(BOUND(MIN(($close - $high), $return), -3, 3))"
    - "-((BOUND(($close - $high), -3, 3) - IF_ELSE((($low - $close) >= 0), ($return * $low), ($high - $dollar_volume))))"
    - "-((ZSCORE($close) * (MAX($return, ($open * $close)) / (TS_STD($dollar_volume, 10) + 1e-8))))"
    - "-(IF((BOUND($dollar_volume, -3, 3) > 0), TS_COVARIANCE(($close - $open), $dollar_volume, 252), CS_RANK($close)))"
    - "-((TS_COV($open, ($return + $dollar_volume), 3) - MAX(($high - $volume), ($volume - $volume))))"
    - "-(POWER(TS_COV(($return * $close), $close, 21), 2))"
    - "(ZSCORE(($close * $low)) - IF((($return * $high) < 0), ($low - $close), IF((($return - $low) > 0), ($return + $close), ($dollar_volume * $high))))"
    - "-(TS_DECAY_LINEAR($volume, 252))"
    - "-(TS_PRODUCT(BOUND(TS_COV($close, MIN($volume, $high), 21), -3, 3), 10))"
    - "-((MIN($dollar_volume, TS_STD($dollar_volume, 5)) - POWER($close, 3)))"
