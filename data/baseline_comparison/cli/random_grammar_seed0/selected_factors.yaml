# Generated by: random-grammar-seed0
# Scored on: daily_bars.parquet (3,279,854 rows after membership filter)
# Membership: /Users/alex/Desktop/Quant_Projects/QuantaAlpha/LLMStrat/data/us_equities/reference/sp500_membership_daily.parquet
# Gates: |t| >= 2.0, |corr| <= 0.7, max 10 factors
# Candidates considered: 54
signals:
  mined_expressions:
    - "-((EMA(($volume * $close), 5) / (IF_ELSE(($open < 0), ($volume + $open), ($close + $dollar_volume)) + 1e-8)))"
    - "-((COUNT(($return >= 0), 5) * IF_ELSE((($high + $high) > $return), $dollar_volume, IF((($volume - $return) > $high), $low, $low))))"
    - "-(TS_COVARIANCE(($close - $volume), ($close - $dollar_volume), 3))"
    - "-(TS_COV(ZSCORE(($volume + $open)), $open, 5))"
    - "-(MIN(($high * $volume), TS_COVARIANCE($return, $close, 63)))"
    - "-(COUNT((BOUND($return, -3, 3) <= 0), 252))"
    - "SIGN(TS_DELTA(($open + $open), 252))"
    - "-(SQRT((TS_DELTA($volume, 21) + COUNT(($return > 0), 3))))"
    - "(RANK(($volume - $dollar_volume)) - (POWER($close, 3) + IF((($dollar_volume + $low) < ($high + $close)), ($volume + $high), $open)))"
    - "BOUND(ABS($low), -3, 3)"
