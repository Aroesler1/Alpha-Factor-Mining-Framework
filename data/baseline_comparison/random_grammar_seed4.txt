# random-grammar-seed4: 54 expressions sampled from
# ExpressionSanitizer's grammar, matched to the Sonnet set's structure.
# Regenerate with scripts/sp500_run_baseline_comparison.py --seeds 5
(DELTA(IF_ELSE(($return > 0), $low, $return), 3) * BOUND($open, -3, 3))
(COUNT(($low <= ($close - $low)), 63) + BOUND($volume, -3, 3))
TS_SUM(POWER($volume, 1), 3)
(MIN(($close - $volume), ($open - $low)) + COUNT((TS_MAX($close, 5) > 0), 3))
TS_COVARIANCE(MAX(($open * $open), ($close - $dollar_volume)), $high, 21)
POWER(MAX((POWER(($low + $high), 0.5) / (COUNT(($close > 0), 3) + 1e-8)), ($return - $volume)), 2)
MIN($volume, ($return + $dollar_volume))
(COUNT(((BOUND(($return - $volume), -3, 3) * TS_COVARIANCE(($close + $return), ($open - $dollar_volume), 3)) > $dollar_volume), 63) + MAX($close, $low))
(ZSCORE($volume) * MIN($close, IF((($volume * $low) >= 0), ($high * $dollar_volume), ($low * $high))))
DELTA((COUNT((($open - $volume) > $open), 5) + TS_COVARIANCE($dollar_volume, $open, 3)), 252)
POWER(POWER(POWER($volume, 1.5), 1.5), 0.25)
COUNT((BOUND(COUNT(($volume > 0), 3), -3, 3) >= ($open + $return)), 21)
MAX(($high + $open), ($open + $high))
BOUND(TS_CORR(($low - $open), ($dollar_volume * $low), 252), -3, 3)
(TS_COVARIANCE($volume, ($open * $dollar_volume), 5) / (IF((($low * $close) > 0), ($low + $volume), $low) + 1e-8))
(IF((($dollar_volume + $open) < 0), ($volume - $return), ($volume + $open)) / (TS_COV(($high * $return), POWER($low, 2), 252) + 1e-8))
POWER(MIN($open, $low), 0.5)
ZSCORE(MIN($high, $close))
POWER(TS_DECAY_LINEAR($high, 252), 2)
CS_DEMEAN($volume)
ABS(($dollar_volume - $high))
(BOUND($close, -3, 3) * IF_ELSE(($return >= ($high * $dollar_volume)), ($high - $high), $close))
IF_ELSE((($low - $volume) > 0), BOUND($low, -3, 3), TS_RANK(($low - $dollar_volume), 252))
MAX(MIN(($close - $return), CS_RANK(($high - $low))), ($return * $high))
MAX(($return * $dollar_volume), IF((($low * $volume) > $dollar_volume), $high, $high))
(RANK(($return - $volume)) * CS_RANK(($low + $high)))
(BOUND($low, -3, 3) * MIN($return, $high))
(COUNT((IF(($return > 0), $high, $high) > ($open * $low)), 63) + DELTA(TS_COVARIANCE(($dollar_volume - $volume), $open, 5), 21))
COUNT((TS_MIN((IF_ELSE(($return < $high), $close, ($close * $dollar_volume)) * ZSCORE($high)), 63) <= ($high + $low)), 10)
(BOUND(COUNT(($return >= ($close + $open)), 3), -3, 3) + ABS($dollar_volume))
POWER($close, 1.5)
IF_ELSE((COUNT((($open - $close) <= ($dollar_volume * $open)), 5) >= 0), ($dollar_volume - $high), COUNT((($close - $volume) <= 0), 63))
TS_DELTA(TS_MAX(($high - $return), 3), 10)
BOUND(BOUND(MIN($low, $return), -3, 3), -3, 3)
TS_COVARIANCE(SIGN($return), $high, 3)
TS_CORR(POWER(MIN(($volume * $close), ($return * $low)), 3), $volume, 10)
(RANK(($dollar_volume - $high)) + MIN(($return * $return), $open))
MIN($open, TS_CORR(($dollar_volume * $close), $volume, 21))
IF((($volume + $open) <= ($dollar_volume * $low)), $high, POWER($close, 3))
(POWER($open, 2) + (LN(POWER(($low + $volume), 2)) * BOUND(($low - $volume), -3, 3)))
TS_CORR(MIN(IF_ELSE((($dollar_volume - $return) <= 0), BOUND(($dollar_volume * $low), -3, 3), $close), $close), TS_ARGMIN(($volume * $open), 10), 63)
(IF_ELSE((($low * $high) < 0), ($close * $dollar_volume), $close) - BOUND(($dollar_volume - $dollar_volume), -3, 3))
POWER((TS_COV(($dollar_volume + $close), $high, 10) + MIN(($open * $close), ($close + $close))), 3)
MIN($open, TS_CORR(($volume - $dollar_volume), ($dollar_volume * $dollar_volume), 3))
POWER((CS_DEMEAN(($close + $return)) / (MAX($low, $dollar_volume) + 1e-8)), 1)
BOUND(IF_ELSE((($volume + $high) >= ($open + $open)), $low, ($dollar_volume + $open)), -3, 3)
TS_COVARIANCE($low, COUNT(($open <= $dollar_volume), 63), 252)
(BOUND($low, -3, 3) * MIN($return, ($high * $high)))
(IF_ELSE((($close - $volume) <= 0), ($low + $return), $return) / (BOUND($high, -3, 3) + 1e-8))
(CS_RANK(IF_ELSE((($high * $open) > 0), ($low + $dollar_volume), ($volume - $volume))) * MIN($dollar_volume, $high))
TS_COVARIANCE(TS_CORR($close, ($close * $high), 3), (COUNT(($high >= $return), 3) + LOG($low)), 21)
POWER((EMA($high, 3) - COUNT((MIN(($open + $volume), $volume) >= $open), 63)), 0.25)
SQRT(TS_RANK(($volume * $open), 63))
MAX($close, MIN(POWER(($low * $return), 2), $open))
